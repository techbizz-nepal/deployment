  map $host $is_ip_host {
    default 1;
    "~*^(\d{1,3}\.){3}\d{1,3}$" 0;
    "~*^\[[0-9a-fA-F:]+\]$" 0;
  }

map $http_user_agent $is_allowed_user_agent {
    default 0;
    "~*Chrome|Firefox|Safari|Edge|Opera|Vivaldi|Brave${ALLOWED_USER_AGENT}" 1;
    "~*Mozilla" 1;
}